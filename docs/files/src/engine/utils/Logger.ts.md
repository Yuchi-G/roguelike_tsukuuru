# src/engine/utils/Logger.ts

- 役割: ゲームログを最大件数つきで保持する。
- 主な状態: `messages`、`maxMessages`。
- 主な処理: `add()` で先頭追加、`all()` で一覧コピーを返す。
- 呼ばれ方: `Game`、アイテム効果、スクリプトから使われる。
- 依存: なし。
- 読むポイント: 新しいログが先頭に入る。
- 読まなくていい部分: なし。
